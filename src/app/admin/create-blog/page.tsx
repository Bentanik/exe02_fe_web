import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Tạo bài viết",
    description: "Tạo bài viết với antiSCM",
};

export default function CreateBlogPage() {
    return (
        <div>
            {/* <CreateBlog /> */}
        </div>
    )
}
