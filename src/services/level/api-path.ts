/* eslint-disable import/no-anonymous-default-export */
const LEVEL = "/level/v1";
const CREATE = LEVEL + "/create-level";
const GET_LEVEL_BY_ID = LEVEL + "/get-level-by-id";
const GET_LEVELS = LEVEL + "/get-levels";

export default {
  CREATE,
  GET_LEVEL_BY_ID,
  GET_LEVELS,
};
